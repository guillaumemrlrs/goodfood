{% extends 'base.html.twig' %}

{% block title %}StatutCommande{% endblock %}

{% block body %}
    <h1>StatutCommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ statut_commande.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ statut_commande.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('statut_commande_index') }}">back to list</a>

    <a href="{{ path('statut_commande_edit', {'id': statut_commande.id}) }}">edit</a>

    {{ include('statut_commande/_delete_form.html.twig') }}
{% endblock %}
