<form method="post" action="{{ path('catalogue_delete', {'idMenu': catalogue.idMenu}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ catalogue.idMenu) }}">
    <button class="btn">Delete</button>
</form>
