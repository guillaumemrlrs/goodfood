<form method="post" action="{{ path('cb_delete', {'idCb': cb.idCb}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cb.idCb) }}">
    <button class="btn">Delete</button>
</form>
