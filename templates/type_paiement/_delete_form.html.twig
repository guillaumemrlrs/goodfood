<form method="post" action="{{ path('type_paiement_delete', {'idTypePaiement': type_paiement.idTypePaiement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_paiement.idTypePaiement) }}">
    <button class="btn">Delete</button>
</form>
