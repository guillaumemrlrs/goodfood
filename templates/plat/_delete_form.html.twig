<form method="post" action="{{ path('plat_delete', {'idPlat': plat.idPlat}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plat.idPlat) }}">
    <button class="btn">Delete</button>
</form>
