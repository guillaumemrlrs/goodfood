{% extends 'base.html.twig' %}

{% block title %}CommandeClient index{% endblock %}

{% block body %}
    <h1>CommandeClient index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Datecreation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for commande_client in commande_clients %}
            <tr>
                <td>{{ commande_client.id }}</td>
                <td>{{ commande_client.datecreation ? commande_client.datecreation|date('Y-m-d H:i:s') : '' }}</td>
                <td>
                    <a href="{{ path('commande_client_show', {'id': commande_client.id}) }}">show</a>
                    <a href="{{ path('commande_client_edit', {'id': commande_client.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('commande_client_new') }}">Create new</a>
{% endblock %}
