{% extends 'base.html.twig' %}

{% block title %}CommandeClient{% endblock %}

{% block body %}
    <h1>CommandeClient</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commande_client.id }}</td>
            </tr>
            <tr>
                <th>Datecreation</th>
                <td>{{ commande_client.datecreation ? commande_client.datecreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_client_index') }}">back to list</a>

    <a href="{{ path('commande_client_edit', {'id': commande_client.id}) }}">edit</a>

    {{ include('commande_client/_delete_form.html.twig') }}
{% endblock %}
