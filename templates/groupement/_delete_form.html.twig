<form method="post" action="{{ path('groupement_delete', {'idGroupement': groupement.idGroupement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ groupement.idGroupement) }}">
    <button class="btn">Delete</button>
</form>
