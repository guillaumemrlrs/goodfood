{% extends 'base.html.twig' %}

{% block title %}Groupement{% endblock %}

{% block body %}
    <h1>Groupement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdGroupement</th>
                <td>{{ groupement.idGroupement }}</td>
            </tr>
            <tr>
                <th>NomGroupement</th>
                <td>{{ groupement.nomGroupement }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('groupement_index') }}">back to list</a>

    <a href="{{ path('groupement_edit', {'idGroupement': groupement.idGroupement}) }}">edit</a>

    {{ include('groupement/_delete_form.html.twig') }}
{% endblock %}
