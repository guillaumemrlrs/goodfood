<form method="post" action="{{ path('type_commande_delete', {'idTypeCommande': type_commande.idTypeCommande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_commande.idTypeCommande) }}">
    <button class="btn">Delete</button>
</form>
