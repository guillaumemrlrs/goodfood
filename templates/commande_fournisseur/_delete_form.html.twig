<form method="post" action="{{ path('commande_fournisseur_delete', {'idCommande': commande_fournisseur.idCommande}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ commande_fournisseur.idCommande) }}">
    <button class="btn">Delete</button>
</form>
