{% extends 'base.html.twig' %}

{% block title %}CommandeFournisseur{% endblock %}

{% block body %}
    <h1>CommandeFournisseur</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdCommande</th>
                <td>{{ commande_fournisseur.idCommande }}</td>
            </tr>
            <tr>
                <th>DateCreation</th>
                <td>{{ commande_fournisseur.dateCreation ? commande_fournisseur.dateCreation|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('commande_fournisseur_index') }}">back to list</a>

    <a href="{{ path('commande_fournisseur_edit', {'idCommande': commande_fournisseur.idCommande}) }}">edit</a>

    {{ include('commande_fournisseur/_delete_form.html.twig') }}
{% endblock %}
